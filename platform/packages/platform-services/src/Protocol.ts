/**
 * Protocol — identificador opaco do protocolo usado por um Connector. Nenhum protocolo recebe
 * tratamento privilegiado na arquitetura; nenhum valor concreto é nomeado aqui.
 * Estrutura definida em INTEGRATION_CONCRETE_STRUCTURE.md.
 */
export type Protocol = string;
