/**
 * Os sete widgets originais da Sprint 8 — um por subsistema consumido —
 * mais `ANALYTICS`, adicionado na Sprint 18 (Dashboard Integration)
 * para o AnalyticsWidget.
 */
export enum DashboardWidgetType {
  RUNTIME = "runtime",
  EVENT_BUS = "event_bus",
  MEMORY = "memory",
  WORKFLOW = "workflow",
  AGENT = "agent",
  KNOWLEDGE = "knowledge",
  AI_GATEWAY = "ai_gateway",
  ANALYTICS = "analytics",
}
