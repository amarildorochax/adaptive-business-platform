// ModuleRegistry.ts
//
// Responsabilidade:
// Futuro registro central dos módulos de negócio (src/modules/*, cada
// um implementando IModule). Permitirá ao PlatformRegistry localizar e
// listar módulos de forma genérica. Sem implementação nesta etapa.

export class ModuleRegistry {
  // Estrutura reservada para registro futuro de módulos (IModule).
}
