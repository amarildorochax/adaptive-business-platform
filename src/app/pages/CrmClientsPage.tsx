// CrmClientsPage.tsx
//
// Responsabilidade:
// Composição de rota de Clientes do CRM (Sprint 33A — rota
// `/crm/clientes`) — liga `ClientsPage` (feature) à rota.

import { ClientsPage } from '@/app/features/crm';

export function CrmClientsPage() {
  return <ClientsPage />;
}
