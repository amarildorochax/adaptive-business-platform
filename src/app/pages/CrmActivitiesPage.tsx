// CrmActivitiesPage.tsx
//
// Responsabilidade:
// Composição de rota de Atividades do CRM (Sprint 33A — rota
// `/crm/atividades`) — liga `ActivitiesPage` (feature) à rota.

import { ActivitiesPage } from '@/app/features/crm';

export function CrmActivitiesPage() {
  return <ActivitiesPage />;
}
