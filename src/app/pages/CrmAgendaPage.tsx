// CrmAgendaPage.tsx
//
// Responsabilidade:
// Composição de rota da Agenda do CRM (Sprint 33A — rota
// `/crm/agenda`) — liga `AgendaPage` (feature) à rota.

import { AgendaPage } from '@/app/features/crm';

export function CrmAgendaPage() {
  return <AgendaPage />;
}
