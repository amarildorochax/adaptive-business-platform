// CrmDealsPage.tsx
//
// Responsabilidade:
// Composição de rota de Negócios do CRM (Sprint 33A — rota
// `/crm/negocios`) — liga `DealsPage` (feature) à rota.

import { DealsPage } from '@/app/features/crm';

export function CrmDealsPage() {
  return <DealsPage />;
}
