// CrmCompaniesPage.tsx
//
// Responsabilidade:
// Composição de rota de Empresas do CRM (Sprint 33A — rota
// `/crm/empresas`) — liga `CompaniesPage` (feature) à rota.

import { CompaniesPage } from '@/app/features/crm';

export function CrmCompaniesPage() {
  return <CompaniesPage />;
}
