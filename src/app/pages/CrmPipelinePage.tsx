// CrmPipelinePage.tsx
//
// Responsabilidade:
// Composição de rota do Pipeline do CRM (Sprint 33A — rota
// `/crm/pipeline`) — liga `PipelinePage` (feature) à rota.

import { PipelinePage } from '@/app/features/crm';

export function CrmPipelinePage() {
  return <PipelinePage />;
}
