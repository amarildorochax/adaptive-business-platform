// CrmOverviewPage.tsx
//
// Responsabilidade:
// Composição de rota do Painel do CRM (Sprint 33A — rota `/crm`,
// índice) — liga `OverviewPage` (feature) à rota.

import { OverviewPage } from '@/app/features/crm';

export function CrmOverviewPage() {
  return <OverviewPage />;
}
