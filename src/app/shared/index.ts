// index.ts
//
// Responsabilidade:
// Ponto único de exportação dos tipos e utilitários compartilhados
// internamente pelo Frontend Foundation (`src/app/*`).
//
// Nota: não confundir com `src/shared/` (interfaces da UI legada do
// "escritório" Phaser/Pixi) — são módulos independentes, sem relação.

export * from './routeMeta';
